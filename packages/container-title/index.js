import ContainerTitle from './src/main.vue';

/* istanbul ignore next */
ContainerTitle.install = function(Vue) {
  Vue.component(ContainerTitle.name, ContainerTitle);
};

export default ContainerTitle;
